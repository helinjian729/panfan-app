# 拼饭 App - 产品需求文档（PRD）

## 1. 项目背景与目标

### 1.1 项目背景

随着外卖平台的普及，白领人群在工作日中午点外卖已成为常态。然而：
- 单人点外卖配送费高昂，凑不够满减优惠
- 同事之间缺乏便捷的拼单渠道，常出现"想吃一样的但没人一起拼"的尴尬
- 人工统计拼单需求耗时费力，效率低下

### 1.2 项目目标

打造一款针对白领午餐场景的拼饭工具，帮助用户轻松发起/参与拼饭团，凑满减、摊配送费，让拼饭更划算、更便捷。

---

## 2. 目标用户与使用场景

### 2.1 目标用户

| 用户特征 | 描述 |
|---------|------|
| 核心人群 | 写字楼白领上班族 |
| 年龄区间 | 22-40 岁 |
| 使用场景 | 工作日午餐拼单 |
| 技术能力 | 熟练使用智能手机 |

### 2.2 典型使用场景

**场景一：发起拼饭**
> 12点前，市场部小李想在楼下湘菜馆点外卖，发现差10元就能满100减30。他拉了一个拼饭团，邀请同楼层同事加入，最终5人拼单，每人省了6元。

**场景二：加入拼饭**
> 产品经理小王收到群消息"还差1人，差18元成团"，点击链接直接加入拼饭团，付款完成。

**场景三：凑单提醒**
> 拼饭团差2人成团，系统自动发送通知提醒未付款用户"还差X元/人即可成团"。

---

## 3. 功能清单与优先级

### 3.1 功能优先级矩阵

| 优先级 | 功能模块 | 功能点 |
|-------|---------|--------|
| **P0 - Must-have** | 拼饭团管理 | 发起拼饭团、加入拼饭团、查看拼饭团列表 |
| **P0 - Must-have** | 外卖选品 | 浏览菜单、加购、凑单提示 |
| **P0 - Must-have** | 订单管理 | 拼单订单创建、支付、订单详情 |
| **P1 - Should-have** | 拼饭团状态 | 成团/失败通知、倒计时、成员管理 |
| **P1 - Should-have** | 历史记录 | 历史拼单记录、常点商家 |
| **P2 - Could-have** | 社交互动 | 拼饭团聊天、分享邀请 |
| **P2 - Could-have** | 偏好推荐 | 基于历史推荐商家/菜品 |

### 3.2 功能详情

#### 3.2.1 拼饭团管理

**发起拼饭团**
- 用户选择商家和菜品，填写拼饭团名称
- 设置目标人数、截止时间（默认30分钟）
- 生成拼饭团邀请链接/二维码
- 支持分享至微信、企业微信、钉钉

**加入拼饭团**
- 通过邀请链接/二维码直接入团
- 入团时显示：已选菜品、当前凑单进度、距截止时间
- 支持搜索附近的拼饭团（按商家/距离筛选）

**拼饭团列表**
- 我发起的拼饭团（待成团/已成功/已取消）
- 我加入的拼饭团
- 附近拼饭团（可加入）

#### 3.2.2 外卖选品

**商家列表**
- 按评分/销量/配送费/距离排序
- 支持搜索商家名称
- 显示商家起送价、满减门槛

**菜品浏览**
- 分类查看菜单
- 加入购物车
- 凑单提示："再选X元，可满足满100减30"

**凑单计算器**
- 自动计算：当前总价、距离满减门槛、每人分摊金额
- 实时更新，拼单成员可看到凑单进度

#### 3.2.3 订单管理

**创建订单**
- 合并拼饭团内所有成员的菜品
- 显示：总价、满减优惠、实付金额、每人分摊
- 支持备注（如：不要辣、加辣）

**支付**
- 支持微信支付、支付宝
- 发起人代付或AA制
- 支付成功后通知所有成员

**订单详情**
- 订单状态：待支付/已支付/已接单/配送中/已完成
- 配送信息：骑手位置、预计送达时间
- 菜品清单、金额明细

---

## 4. 业务流程图

### 4.1 拼饭主流程

```
[用户A-发起人]                    [用户B/C/D-参与者]
      |                                  |
      |-- 选择商家，添加菜品 ------------>|
      |-- 填写拼饭团信息（名称/人数/时长）|
      |-- 生成邀请链接                   |
      |                                  |
      |<-- 分享邀请链接 ------------------|
      |                                  |
      |          [拼饭团已创建]            |
      |                                  |
      |<-- 点击链接加入拼饭团 -------------|
      |<-- 选择商家，添加菜品 -------------|
      |                                  |
      |     [所有成员菜品已添加完毕]        |
      |                                  |
      |-- 确认订单，支付 ---------------->|
      |-- 通知商家接单                    |
      |-- 通知骑手取餐                    |
      |                                  |
      |<-- 配送中，实时位置更新 ----------|
      |<-- 确认送达 ---------------------|
      |                                  |
      [订单完成]                         [订单完成]
```

### 4.2 凑单流程

```
用户添加商品到购物车
      |
      v
系统计算：当前总价 vs 满减门槛
      |
      +-- 已满足 --> 显示"已满足满减条件"
      |
      +-- 未满足 --> 计算"再选X元即可满足"
                       |
                       v
                  高亮推荐凑单品
                       |
                       v
                  用户添加凑单品
                       |
                       v
                  重新计算，循环直到满足条件
```

---

## 5. 字段定义与枚举规范

### 5.1 拼饭团

| 字段名 | 类型 | 说明 |
|--------|------|------|
| groupId | String | 拼饭团唯一ID |
| groupName | String | 拼饭团名称（如"市场部湘菜拼饭团"） |
| creatorId | String | 发起人用户ID |
| restaurantId | String | 商家ID |
| targetCount | Int | 目标人数（不含发起人） |
| currentCount | Int | 当前已加入人数 |
| status | Enum | 拼饭团状态：PENDING/SUCCESS/FAILED/CANCELLED |
| deadline | DateTime | 截止时间 |
| inviteCode | String | 邀请码（6位） |
| shareUrl | String | 分享链接 |
| totalAmount | Decimal | 订单总金额 |
| discountAmount | Decimal | 满减优惠金额 |
| finalAmount | Decimal | 实付金额 |
| createdAt | DateTime | 创建时间 |

### 5.2 拼饭团成员

| 字段名 | 类型 | 说明 |
|--------|------|------|
| memberId | String | 成员ID |
| groupId | String | 所属拼饭团ID |
| userId | String | 用户ID |
| nickname | String | 昵称 |
| avatarUrl | String | 头像URL |
| items | List\<OrderItem\> | 该成员所选菜品 |
| itemsAmount | Decimal | 成员菜品小计 |
| payAmount | Decimal | 成员实际支付金额 |
| status | Enum | 状态：JOINED/PAID/CANCELLED |
| joinedAt | DateTime | 加入时间 |

### 5.3 订单项

| 字段名 | 类型 | 说明 |
|--------|------|------|
| itemId | String | 菜品ID |
| name | String | 菜品名称 |
| price | Decimal | 单价 |
| quantity | Int | 数量 |
| subtotal | Decimal | 小计 |
| remark | String | 备注（如：不要辣） |

### 5.4 枚举定义

```swift
// 拼饭团状态
enum GroupStatus {
    PENDING   // 待成团
    SUCCESS   // 已成团
    FAILED    // 成团失败（超时/人数不够）
    CANCELLED // 已取消
}

// 订单状态
enum OrderStatus {
    PENDING_PAYMENT  // 待支付
    PAID            // 已支付
    ACCEPTED        // 已接单
    DELIVERING      // 配送中
    DELIVERED       // 已送达
    COMPLETED       // 已完成
    REFUNDED        // 已退款
}
```

---

## 6. 异常场景与处理

| 异常场景 | 处理规则 |
|---------|---------|
| **拼饭团超时未成团** | 自动取消拼饭团，通知所有已支付用户退款 |
| **成员中途退出** | 若退出后不满足成团条件，通知剩余成员凑单 |
| **商家拒单** | 通知所有成员，取消订单，全额退款 |
| **配送异常** | 通知成员，联系骑手/商家处理，必要时退款 |
| **支付失败** | 展示支付失败原因，保留订单5分钟内可重试 |
| **发起人取消拼饭团** | 通知所有已加入成员，原路退款 |
| **菜品售罄** | 通知该成员删除/替换菜品，重新计算金额 |

---

## 7. 非功能需求

### 7.1 性能需求

| 指标 | 要求 |
|-----|------|
| 页面加载时间 | 首页 < 2秒，列表页 < 1.5秒 |
| 拼单响应实时性 | 成员添加菜品后，其他成员端 < 500ms 同步更新 |
| 并发支持 | 单个拼饭团支持最多20人同时操作 |

### 7.2 兼容性需求

| 项目 | 要求 |
|-----|------|
| iOS 版本 | 支持 iOS 14.0 及以上 |
| 设备适配 | iPhone 全系列（SE/标准/Plus/Pro Max） |
| 网络要求 | 支持 4G/5G/WiFi，断网时本地缓存操作 |

### 7.3 推送通知

| 通知场景 | 通知方式 |
|---------|---------|
| 拼饭团邀请 | 微信/系统推送 |
| 成团/失败通知 | 系统推送 |
| 凑单进度提醒 | 系统推送 |
| 订单状态更新 | 系统推送 |

---

## 8. 版本迭代计划

### MVP 版本（P0 功能）

```
版本号：V1.0
发布时间：T+4周

包含功能：
1. 用户登录（手机号+验证码）
2. 发起拼饭团（选择商家→添加菜品→生成邀请）
3. 加入拼饭团（扫码/链接入团）
4. 拼饭团列表（发起的/加入的）
5. 订单创建与支付
6. 订单详情与状态追踪
7. 基础消息通知

不包括：社交聊天、偏好推荐、评价系统
```

### V2.0 迭代计划

| 功能 | 说明 |
|-----|------|
| 附近拼饭团 | 基于位置发现附近可加入的拼饭团 |
| 常点商家 | 记录用户高频商家，一键发起 |
| 分享海报 | 生成精美拼饭邀请海报 |

### V3.0 迭代计划

| 功能 | 说明 |
|-----|------|
| 拼饭团聊天 | 拼饭团内成员实时聊天 |
| 智能推荐 | 基于历史推荐商家/凑单品 |
| 优惠券中心 | 平台发放满减优惠券 |

---

## 9. 竞品分析（可选）

| 竞品 | 优点 | 不足 |
|-----|------|------|
| 美团拼单 | 商家资源丰富 | 拼单入口深，功能分散 |
| 饿了么拼单 | 配送网络成熟 | 社交属性弱 |
| 内部工具（如企业微信拼单群） | 同事间信任度高 | 人工统计繁琐，无系统化 |

**我们的差异化定位**：
- 专注"拼饭"单一场景，体验更轻量
- 强调"凑单计算器"，让满减更透明
- 轻社交，优先满足凑单需求

---

*文档版本：V1.0*
*创建时间：2026-05-06*
*产品经理：AI Agent*
